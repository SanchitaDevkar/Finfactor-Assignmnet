spring.application.name=weather-backend

# Server
server.port=8080

# Database
spring.datasource.url=jdbc:mysql://localhost:3307/weatherdb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Satara@123

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# External API
weather.api.key=${WEATHER_API_KEY}
weather.api.url=https://api.openweathermap.org/data/2.5/weather

# Increase housekeeper frequency (check every 60 seconds instead of default 30)
spring.datasource.hikari.housekeeping.period=60000
